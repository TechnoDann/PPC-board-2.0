<p>
Hello <%= @resource.name %>!

Someone has requested password change link for your account. You can change your password through the link below.</p>

<p>
<%= link_to edit_password_url(@resource, :reset_password_token => @resource.reset_password_token), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %>
</p>

<p>
If you didn't request this link, please ignore this email.
Your password won't change until you access the link above and change it.</p>
